unit uVariaveis;

interface

Uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  Menus, ComCtrls, ExtCtrls, StdCtrls, dbtables, Typinfo, SqlExpr,
  IniFiles, Registry, ActiveX, ComObj, ShlObj, Winsock, Variants, DateUtils;

var
  NomeUsuarioLogado: string;
  StringConexao: string;
  EnderecoCondominio: string;
  NomeCondominio: string;


implementation

end.
